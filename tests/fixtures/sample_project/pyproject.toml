[project]
name = "sample-project"
requires-python = ">=3.11"

[tool.ruff]
line-length = 88
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "B", "C90"]

[tool.ruff.lint.mccabe]
max-complexity = 10
