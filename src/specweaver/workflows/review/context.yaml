# Copyright (c) 2026 sbula. All rights reserved.
# Licensed under the Apache License, Version 2.0. See LICENSE file in the project root.

name: review
level: module
purpose: >
  LLM-powered spec and code review with structured verdicts (ACCEPTED/DENIED).
  Supports constitution injection for project-rule-aware reviews.

archetype: orchestrator

consumes:
  - specweaver/llm  # Pure logic modules must not depend on LLMs or prompt generation.
  - specweaver/config

forbids:
  - specweaver/sandbox/*  # The loom boundary isolates execution (git, filesystem, processes). High-level orchestrators must never bypass the flow engine to natively execute raw processes.

exposes:
  - Reviewer
  - ReviewResult
  - ReviewVerdict
  - ReviewFinding

# -- Operational Metadata --------------------------------------------------
operational:
  async_ready: false
  concurrency_model: none
