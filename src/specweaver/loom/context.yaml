# Copyright (c) 2026 sbula. All rights reserved.
# Licensed under the Apache License, Version 2.0. See LICENSE file in the project root.

name: loom
level: meta-module
purpose: >
  Engine layer providing agent tools, workflow atoms, and shared infrastructure for git and filesystem operations.

description: |
  The Loom is SpecWeaver's execution engine. It is organized into three sub-layers:
  - tools/ — agent-facing capability providers (role-restricted interfaces)
  - atoms/ — engine-internal workflow operations (unrestricted)
  - commons/ — shared infrastructure used by both tools and atoms

archetype: orchestrator

consumes:
  - specweaver/loom/tools/*  # Atoms and raw orchestrators must not execute intent-gated agent tools directly.
  - specweaver/loom/atoms/*  # Agent-facing tools and commons must not access internal unrestricted engine atoms.
  - specweaver/loom/commons/*  # High-level models must not execute raw executors directly; they must use atoms.

forbids: []

exposes:
  - tools
  - atoms
  - commons

# -- Operational Metadata --------------------------------------------------
operational:
  async_ready: false
  concurrency_model: none
