# US-13: Financial-Grade Math Proofs

*Paths and seams for this (sub)story. `ADR-005`: integration is implicit here — a path this story
cannot walk with one feature is a **seam FR** of the story, and its test is written red first. Where
a related (sub)story is unbuilt the test is still written now, as `xfail(strict=True)` naming the
blocker. The story is finished when those tests are green.*

*Requirement ids of the shape `INT-US-NN FR-N` are frozen history: they name delivered requirements
cited by tests and design documents, and renaming them would break the citations without changing
what was built. No new one is ever minted.*

## Base Story
* **Status:** ⬜ Nothing built — this story holds no delivered capability, so it owes no proof yet
* **Integration Description:** written when the first capability of Financial-Grade Math Proofs is designed. `ADR-005`:
  the paths are listed then, and each spanning test is written **red first** — that is the one
  moment it is available, and it is still ahead rather than behind.
* **Verifiable Proof:** none owed. Not a gap: a story with nothing built has nothing to prove, and
  recording it as pending proof would put it in a queue it does not belong in.

## Sub-Story Add-Ons

* No explicit sub-story contracts defined yet.
