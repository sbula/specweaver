# Topic 07: Technical Debt & Architecture (TECH)

This document tracks all massive refactoring efforts, technical debt removal, and underlying architectural epics required to ensure the platform remains stable, secure, and mathematically sound as it scales to enterprise levels. These stories do not add new user-facing features but are critical for long-term project viability.

## Domain-Driven Design (DDD)
* **`TECH-001` 🟢: Domain-Driven Design Unification**
  > [Description](../features/topic_07_technical_debt/TECH-001/TECH-001_design.md) | SpecWeaver's internal architecture is cohesive and microservice-ready, preventing "Dumping Ground" anti-patterns and circular dependencies as the team scales. The refactoring effort aligned the legacy `config/`, `cli/`, and `loom/` layers with pure Domain-Driven Design (Package by Feature) principles established by the B-SENS-02 Graph Triad. SF-04 (2026-08-02) eliminated all three live circular dependencies — `core.config ⇄ infrastructure.llm`, `core.config ⇄ core.flow`, and a third (`core.config ⇄ workspace`) found during SF-04's own review — by relocating the two cross-domain-touching files into a new `core.config.bootstrap` sub-package; `core.config` now has `depends_on = []` in `tach.toml`, matching its own `context.yaml`'s `consumes: []` claim for the first time. `TECH-025` tracks a separate, pre-existing FR-traceability gap in SF-01/02/03's citations (found by the closure gate, unrelated to this claim).

* **`TECH-025` 🟢: Registry IDs Leaking Into Proofs — FR Traceability Gap and Story-Named Tests (TECH-001 SF-01/02/03, TECH-002 SF-01..4, TECH-005 SF-01/2)**
  > [Description](../features/topic_07_technical_debt/TECH-025/TECH-025_design.md) | _(2026-08-02, widened same day — found running `check_fr_coverage.py TECH-001` as SF-04's closure gate, then again running `check_fr_coverage.py TECH-005` as SF-03's closure gate; widened a second time 2026-08-08 when verifying TECH-002's status)_ | None of `TECH-001_design.md`'s FR-1–8 (SF-01/02/03), `TECH-002_design.md`'s FR-1–6 (all four sub-features) or `TECH-005_design.md`'s FR-1–7 (SF-01/2) are cited by the literal string `FR-N` in any implementation plan or test file naming their story — the citation *convention* was never followed for either, most plausibly because this gate didn't exist yet when they shipped. Not evidence the underlying work is wrong (all three stories' own `Verifiable Proof` test suites pass); a documentation/citation gap, not a functional one. TECH-001 SF-04's FR-9 and TECH-005 SF-03's FR-8 are both correctly cited and unaffected. **Widened a third time (2026-08-08, at design)** to the test-naming half: nine test files and three test functions are named after the story that paid for them, which `check_conventions.py` R5 already forbids and had to grandfather pending "a ticket that decides both halves together". Same root cause — a registry ID leaking into something that outlives it. **DELIVERED 2026-08-12** across seven sub-features. Five ledgers now exit 0 — `TECH-001` (SF-04), `TECH-002` (SF-05), `TECH-005` (SF-06), `INT-US-21`, and TECH-025's own (SF-07) — with `scripts/baselines/fr_traceability_closed.txt` and a guard test that fails if any reopens. SF-01 first closed a hole where the gate credited its own checker's fixture inputs; SF-02/SF-03 renamed nine story-named test files and three functions, widened R5 to every tier and name kind, and added the R6 class-naming ratchet. **The recurring finding, hit four times:** the citation scan attributes whole-file, so a file naming one story is credited every requirement token in it — SF-04 found a live false credit after it had paid out, SF-05 caught one before it could, SF-06 found the plan-side twin where a disclaimer in the wrong sub-feature's plan read as ownership, and SF-07 hit it as a hard design constraint that forced the closed-story ids into data the guard reads at run time. Verifying by **file list rather than exit code** is what caught the last three.
* **`TECH-002` 🟢: BaseTool Registry Refactoring**
  > [Description](../features/topic_07_technical_debt/TECH-002/TECH-002_design.md) | Eliminates manual tool registration and automates dependency injection bindings for all sandbox tools via an explicit `ToolRegistry` (`sandbox/registry.py`). Automatic registration via `__init_subclass__` was never implemented — the approved design deliberately rejected it (global import-time auto-registration pollutes memory space and bypasses isolation limits) in favor of the explicit registry actually shipped. Status corrected because the mechanism this entry originally described never existed.

## Architecture & Restructuring
* **`TECH-020` 🟢: Extract the Step-Execution Loop from PipelineRunner**
  > [Description](../features/topic_07_technical_debt/TECH-020/TECH-020_design.md) | _Status: STUB. Origin: INT-US-21 SF-03 CB-2 pre-commit gate, finding A5 (2026-07-27)._ | `core/flow/engine/runner.py` sits at **exactly 600 lines against a 600-line RED threshold** — the next line added blocks whatever commit adds it. That already happened: CB-2's one-line `current_run_id` accessor pushed it to 601 and the gate blocked; the fix was condensing the *comment*, so today's headroom is bought with prose density rather than structure and the next contributor pays with no warning. The real measurement is worse than the line count: **`_execute_loop` spans lines 215–575 — 360 lines, ~60% of the file, in ONE method whose complexity is silenced with `# noqa: C901`**. That suppression is the load-bearing part — the file-size threshold is a proxy, but the `noqa` is a direct admission the method is past the project's own complexity bar. Three modules were already extracted from this file during INT-US-21 (`engine/hydration.py`, `engine/approval.py`, `engine/staleness.py`), each time because a feature needed room — three data points saying the *loop*, not the file, needs splitting. Candidate seams: per-step execution (lookup → execute → hydrate → gate → route) as a collaborator, and the loop-back/retry bookkeeping where `NFR-2`'s inherited per-session `attempts` reset lives. **The `# noqa: C901` must be removed, not relocated** — a suppression that survives means the refactor did not happen. Strictly structural: no behaviour change, every existing test passes untouched, and the handover/telemetry `finally:` contract preserved exactly (`TECH-017` flags graceful shutdown as under-tested, so do not disturb it blind). Own commits, never bundled into a feature — same rule as `TECH-016`. Sequence **before** `C-FLOW-12`'s fan-out work, or that feature pays the tax first.

* **`TECH-003` 🟢: Structural Refactoring of Workspace AST Module**
  > [Description](../features/topic_07_technical_debt/TECH-003/TECH-003_design.md) | To make the bounded context crystal clear, we want to introduce a dedicated `ast` boundary inside the workspace module. This separates mechanical Tree-Sitter extraction (`parsers`) from output mapping (`adapters`).
* **`TECH-004` 🟢: Architectural Refactoring of `graph/` Bounded Context**
  > [Description](../features/topic_07_technical_debt/TECH-004/TECH-004_design.md) | Resolved: the standalone CLI command for graph building is **not** an architectural violation. Design doc `AD-2` decided to keep the CLI as the Composition Root rather than centralize orchestration into a `GraphBuildAtom` or an autonomous `spinUp` workflow — no such Atom exists in `src/`, matching that decision. _(2026-07-31: reworded — this previously read as an open question when the decision has been settled and shipped.)_

* **`TECH-016` 🔴: Unified Artifact Writer & Serialization Format Enforcement**
  > [Description](../features/topic_07_technical_debt/TECH-016/TECH-016_design.md) | _(new, 2026-07-25 — found during INT-US-21 SF-02 implementation-plan Phase 0)_ | Two handlers dump a Pydantic model straight to YAML (`generation.py:398` `PlanSpecHandler`, `scenario.py:96` `ConvertScenarioHandler`) via `yaml.dump(x.model_dump(), buf)`. Both are safe **only because their models happen to have no enum fields** — `model_dump()` returns Python objects, so any enum reaches `ruamel` as an enum and it raises `RepresenterError`. Measured on `DecompositionPlan`, whose `components[].proposed_dal: DALLevel` is **required**, the failure rate would be 100% of real plans; `model_dump(mode="json")` dumps cleanly. Add one enum field to `PlanArtifact` or `ScenarioSet` and both existing writers break, with no test to catch it. Separately, the `derive path → uuid → tag → write → lineage` sequence is hand-rolled **five** times (`draft.py` ×2, `generation.py`, `lint_fix.py` ×2), and INT-US-21 SF-02 adds a sixth. Fix: one shared artifact-writer helper, **plus** an assertion in the existing `tests/unit/test_architecture.py` that no source file passes a bare `.model_dump()` into `yaml.dump(` — the helper alone is only *available*, the check is what makes it *required*. SF-02 writes `mode="json"` inline meanwhile, so nothing is blocked. Land as its own commits, never bundled into a feature commit.

* **`TECH-015` 🔴: Retire Grab-Bag Modules (Name-Says-Nothing Refactor)**
  > [Description](../features/topic_07_technical_debt/TECH-015/TECH-015_design.md) | _(new, 2026-07-25 — user mandate during INT-US-21 SF-01 CB-4)_ | A module whose name promises nothing cannot be contradicted, so it accretes: every author with something that "sort of belongs near the runner" reaches for `runner_utils`. That is the mechanism, and it is observable — INT-US-21 CB-4 added `try_staleness_bypass` to it for exactly that reason (relocated to `engine/staleness.py` in the same commit boundary). Split each offender into modules named for their **contract**, so the next addition has something to violate.
  >
  > **In scope (4 modules, measured):**
  > 1. **`core/flow/engine/runner_utils.py`** — 413 lines, **9 unrelated concerns**: isolation policy (`resolve_should_isolate`, `apply_session_policy`, `_dal_requires_isolation`, `_derive_allowed_paths`), sandboxed execution (`execute_in_sandbox`, `setup_sandbox_caches`), session-worktree lifecycle (`execute_run`), fan-out (`run_fan_out`), the event protocol (`RunnerEventCallback`), vault security (`verify_vault_security`), telemetry (`flush_telemetry`), time formatting (`_now_iso`). Target: `isolation.py`, `session.py`, `fan_out.py`, `events.py`, `security.py`, `telemetry.py`. **32 references across 8 files**; `tests/unit/core/flow/engine/test_runner_utils.py` splits the same way.
  > 2. **`core/flow/handlers/base.py`** — 250 lines. `StepHandler` (Protocol) legitimately belongs in a `base`; `RunContext`, `_now_iso`, `_error_result` and `_build_base_prompt` do not. **Do not rename this one — move the non-base members out.** `_now_iso` has **119 call sites**, most importing it from here rather than from `runner_utils`; relocating it to the L0 `commons` leaf is architecturally right but has the widest blast radius, so it should be its own step. Coordinate with **`TECH-006`**, whose Finding 3 (RunContext god object) targets the same file — do not let the two collide.
  > 3. **`workspace/project/_helpers.py`** — 172 lines, 3 concerns: constitution discovery/loading (`ConstitutionInfo`, `load_constitution`), directory traversal (`walk_up_dirs`), markdown table rendering (`build_tech_stack_rows`, `build_standards_section`).
  > 4. **`interfaces/cli/_core.py`** — 88 lines, 3 concerns: repo-op wrapper (`run_repo_op`), active-project guard (`_require_active_project`), version callback (`_version_callback`).
  >
  > **Explicitly OUT of scope** — these are legitimate single-ABC homes where `base` *is* the contract, and renaming them would be churn: `workspace/ast/parsers/base.py` (`BaseTreeSitterParser`), `infrastructure/llm/adapters/base.py` (`LLMAdapter`), `sandbox/base.py` (`AtomStatus`/`AtomResult`/`Atom`/`BaseTool` — one cohesive abstract kernel).
  >
  > **Guardrail to add with the fix:** a standards rule rejecting new module names matching `util(s)`/`helper(s)`/`misc`/`shared`/`common` outside the L0 `commons` leaf, so this cannot regress. Purely mechanical move+rename per module — land one module per commit, full suite green each time, and do NOT bundle it into a feature commit.

* **`TECH-009` 🟢: Git & Filesystem Subprocess Migration to SubprocessExecutor**
  > [Description](../features/topic_07_technical_debt/TECH-009/TECH-009_design.md) | _Status: DESIGN_COMPLETE. Origin: `E-EXEC-01` backlog._ | Routes the raw-`subprocess` call sites in `git/core/executor.py` and `filesystem/core/search.py` through `SubprocessExecutor` via constructor/parameter DI, clearing the repo-wide `TID251` (banned raw `subprocess`) violations. **Fully implemented** — landed as a side effect of `C-EXEC-02 SF-01`'s pre-commit gate. `cli_drift.py` and `assurance/standards/discovery.py` carry permanent, documented `noqa` exemptions by design, not remaining work: routing them through `sandbox` would cross a bounded-context line that does not exist today. _(2026-07-31: reworded — "Partially implemented" previously read as in-progress when the ticket is complete and the two exemptions are deliberate.)_
  > ⚠️ **ID-collision note (see ADR-002):** the 2026-07-12 3-digit rename (`e73a58c9`) shifted the pre-scheme `TECH-08` — a *reference-only* ID in `adr_002` for an **ABANDONED** `ApplicationService`-layer proposal — onto `TECH-009`, while this design doc was added in the *same commit* and also took `TECH-009`. **This ticket is the live meaning of `TECH-009`.** The abandoned proposal has no design doc and no live ID.

* **`TECH-010` 🔴: MCP Persistent-Process Executor Migration**
  > [Description](../features/topic_07_technical_debt/TECH-010/TECH-010_design.md) | _Status: STUB. Origin: `C-EXEC-02 SF-01` pre-commit `TID251` audit (2026-07-13)._ | `mcp/core/executor.py` keeps a documented raw-`subprocess` exemption: its persistent, bidirectional process pattern is architecturally incompatible with `SubprocessExecutor.execute()`'s one-shot design. Needs a long-lived-process executor abstraction rather than a mechanical migration.

* **`TECH-011` 🔴: Load-Time Params Validation for All Pipeline Step Types**
  > [Description](../features/topic_07_technical_debt/TECH-011/TECH-011_design.md) | _Status: STUB. Origin: `C-EXEC-02 SF-02` implementation-plan Phase 4, Q1 (2026-07-14)._ | `PipelineStep.params` is opaque to `PipelineDefinition.validate_flow()`, so every step type's params are validated only when the step **executes** — potentially far into a long HITL-gated run. Combined with Pydantic `extra="ignore"`, an author typo (e.g. `script:` at step level instead of under `params:`) surfaces as a confusing runtime handler error instead of an immediate load-time failure. Must apply to **all** step types uniformly, not as a bash-specific special case.

* **`TECH-023` 🔴: Repo-Wide Cyclomatic Complexity Violations (complexipy)**
  > [Description](../features/topic_07_technical_debt/TECH-023/TECH-023_design.md) | _(new, 2026-08-02 — found running `quality.py cb` for TECH-001 SF-04, confirmed chronic and unrelated via `git stash`)_ | `complexipy` (threshold 15) fails for **98 functions across 68 files**, spanning nearly every domain — not localized to any recent feature. Worst offenders: `OrchestrateComponentsHandler::execute` (79, `handlers/decompose.py`), `drift_check_rot` (51, `cli_drift.py`), `find_by_glob` (49, `sandbox/filesystem/core/search.py`). Excludes `PipelineRunner::_execute_loop` (`TECH-020`'s target) and `RunContext::model_post_init` (`TECH-006` SF-02's target) — tracked there, not duplicated here.

* **`TECH-024` 🔴: Repo-Wide Dependency Cycles (check_coupling)**
  > [Description](../features/topic_07_technical_debt/TECH-024/TECH-024_design.md) | _(new, 2026-08-02 — found running `quality.py cb` for TECH-001 SF-04, confirmed chronic and unrelated via `git stash`)_ | `check_coupling.py --cycles-only` reports 4 live import cycles: `assurance.validation.registry`/`rules.code.register`/`rules.spec.register` (3); `core.flow.engine.runner`/`runner_utils`/`staleness`/`handlers.decompose`/`handlers.dual_pipeline`/`handlers.registry` (6, overlaps `TECH-020`/`TECH-015`'s files — coordinate sequencing, this ticket owns only the import-direction defect); `infrastructure.llm.adapters._rate_limit`/`factory` (2); `interfaces.api.app`/`ui.htmx`/`v1.pipelines`/`v1.router`/`v1.ws` (5).

## Schema & Data Layer
* **`TECH-005` 🟢: Database Table Prefix Harmonization**
  > [Description](../features/topic_07_technical_debt/TECH-005/TECH-005_design.md) | Every table in the SQLite database now follows the domain-prefix naming convention (e.g., `workspace_projects`, `flow_artifact_events`), established during B-INTL-09 with the `memory_` prefix pattern. SF-03 (2026-08-02) renamed the six raw-`CREATE TABLE` tables SQLAlchemy never managed — `nodes`/`edges` → `graph_nodes`/`graph_edges`, `pipeline_runs`/`audit_log`/`state_schema_version` → `flow_*`, `sw_reservations` → `flow_reservations` — with a zero-data-loss migration path for pre-SF-3 installations. All three sub-features are now committed. `TECH-025` tracks a separate, pre-existing FR-traceability gap in SF-01/2's citations (found by the closure gate, unrelated to this claim) that must close before this story can be marked fully complete.

## Context Loading & RunContext Anti-Patterns
* **`TECH-021` 🟢: `loop_back` Discards the Failing Step's Result** — FIXED 2026-07-28
  > [Description](../features/topic_07_technical_debt/TECH-021/TECH-021_design.md) | _Status: STUB. Origin: INT-US-21 SF-03 CB-3 e2e (2026-07-28) — found by the first test to drive a bundled pipeline through a HITL gate._ | When a gate's `on_fail: loop_back` fires, the failing step's **result is discarded**: the record is left `status=running, result=None`, so the reason is never persisted and never shown. Measured on `feature_decomposition` with a spec failing 4 rules (S06, S07, S09, S10) — session 2 approves the draft gate, `validate_feature` FAILS, the gate loops back, and the run parks at **draft** again with `validate_feature: status=running, result=None`. Sessions 3 and 4 are identical. The user is parked at the wrong gate with nothing anywhere telling them their spec failed validation or which rules failed; every resume looks like the first one. Engine-wide `loop_back` behaviour, not specific to this pipeline — any `on_fail: loop_back` loses its diagnostics the same way. Fix: persist the failing `StepResult` before the rewind (the field exists, it is simply not written on this path), give the looped-back step a status a human would expect (`running` is wrong — nothing is running), and surface the retained reason at the park. **Compounds with, but is separate from, `C-FLOW-07`:** `_execute_loop` re-initialises `attempts` on every entry so each `sw run --resume` grants a fresh 3-strike budget, which INT-US-21 `NFR-2` records as inherited and delegates — do not fix that here. **RESOLVED:** `_handle_loop_back` now retains the failing step's status and result before rewinding (`gates.py`), so the record carries the reason instead of `status=running / result=None`. The strict `xfail` did its job exactly as designed — it flipped to `XPASS(strict)` the instant the fix landed, which is what signalled the marker could be removed; the test is now an ordinary passing assertion. Fixed in its own commit, outside any feature boundary.

* **`TECH-006` 🟢: Context Loading Pipeline Refactoring**
  > [Description](../features/topic_07_technical_debt/TECH-006/TECH-006_design.md) | Three interrelated anti-patterns discovered during D-INTL-06 Red Team analysis that compound as new context sources are added:
  >
  > **Finding 1 — Business Logic in Interface/CLI Layers:** `_load_constitution_content()` is defined in `workspace/project/interfaces/cli.py` and `_load_standards_content()` in `assurance/standards/interfaces/cli.py`. These are data loading utilities, not CLI commands. They should live in their respective domain modules (`workspace/project/constitution.py`, `assurance/standards/loader.py`). The standards loader additionally couples to the CLI singleton via `_core.get_db()`.
  >
  > **Finding 2 — Cross-Interface Spider Web (10+ violations):** The private helpers `_load_constitution_content`, `_load_standards_content`, and `_run_workspace_op` are imported by 10+ other `interfaces/cli.py` modules (including `flow/interfaces/cli.py`, `review/interfaces/cli.py`, `implementation/interfaces/cli.py`, `api/v1/implement.py`, `api/v1/review.py`). These functions are not in any module's `exposes` list — they are private (`_` prefixed) helpers shared across unrelated interface modules, creating a spider web of cross-boundary imports.
  >
  > **Finding 3 — RunContext God Object (23 fields):** `RunContext` in `core/flow/handlers/base.py` has 23 fields and a 67-line `model_post_init` with side-effect-heavy initialization. Every new feature adds a field (`constitution`, `standards`, `plan`, `project_metadata`, `workspace_roots`, `api_contract_paths`, `db`, `llm_router`...). This is a classic God Object / grab-bag anti-pattern.
  >
  > **Highest-ROI Refactoring:** After D-INTL-06 SF-02 lands the shared `build_base_prompt()` factory in `workflows/commons/`, move constitution and standards loading INSIDE the factory (same pattern as memory hydration). This eliminates 2 RunContext fields, 10+ cross-interface imports, and makes the factory the single source of truth for all prompt context injection. Estimated effort: ~4h. Unblocks clean integration of all future context sources (conversation history, knowledge graph snippets).
  >
  > | Sub-Refactoring | What it fixes | Effort | ROI | Dependency |
  > |---|---|---|---|---|
  > | Move `_load_constitution_content` to `workspace/project/constitution.py` | Finding 1 | Low (~30 min) | Medium | None |
  > | Move `_load_standards_content` to `assurance/standards/loader.py` | Finding 1 | Low (~30 min) | Medium | None |
  > | Extract `_run_workspace_op` to a shared utility | Finding 2 | Medium (~2h) | Medium | Requires careful planning |
  > | Load constitution/standards inside factory | Findings 1+2+3 | High (~4h) | **Very High** | After D-INTL-06 SF-02 |
  >
  > ⚠️ **Direction update (2026-07-21, middle-way review):** Findings 1–2 (misplaced loaders, cross-interface import spider-web) and Finding 3 (RunContext god object) remain fully valid — but the recommended **destination** changed. Do NOT centralize content loading inside the prompt factory: under `C-INTL-06` (Envelope-vs-Content) + `C-FLOW-11` (work units read mounted files), constitution/standards move to **domain loaders + canonical on-disk files** that both `oneshot` slots and agentic work units consume. Centralizing into the factory would deepen the slot machinery the new direction narrows. Re-scope the "Highest-ROI" row accordingly at design time.
  >
  > **Status:** Findings 1 and 2 are done (SF-01, committed). Finding 3 is not: `RunContext` has grown to 32 fields with a 68-line `model_post_init`, worse than the 23-field baseline documented above. SF-02 (added to the design doc) closes it.

* **`TECH-014` 🟢: Fan-Out RunContext Isolation (Concurrent Sub-Run State Corruption)**
  > [Description](../features/topic_07_technical_debt/TECH-014/TECH-014_design.md) | _(new, 2026-07-25 — found during INT-US-21 SF-01 CB-2 pre-commit)_ | `OrchestrateComponentsHandler` hands **the same `RunContext` object** to every concurrent sub-runner (`handlers/decompose.py:238-241`, `context=context.pipeline_runner._context`), while `PipelineRunner._execute_loop` writes `run_id`, `step_records` and `pipeline_runner` onto that context on **every step** (`engine/runner.py:404-406`). With N sub-runs executing concurrently these writes interleave, so a sub-run can observe another's `run_id` — **lineage and telemetry events are attributable to the wrong sub-run today**, and `step_records`/handover are likewise corruptible. INT-US-21 FR-2 widened the blast radius to `context.plan` / `context.decomposition` (the hydration hook now writes them at the same shared object) but **did not create the defect** — it is live in shipped `C-FLOW-03` fan-out. Fix: give each sub-run its own context (deep/derived copy at the fan-out boundary, or thread run-scoped state through the call instead of the context), then pin it with a concurrency test asserting per-sub-run `run_id` attribution. **Deliberately NOT deferred to `C-FLOW-12`/`INT-US-21-SF02`**: that add-on is DAG *execution* + DAL isolation and should be able to *assume* context hygiene, and it is sequenced behind unbuilt `C-EXEC-07` — gating a live shipped-code defect on it would leave corrupt telemetry in place indefinitely. Blocks nothing in the INT-US-21 base contract; **should land before `C-FLOW-12`**.

## Security & Validation
* **`TECH-007` 🟢: PromptBuilder Input Escaping & Pluggable Context Architecture**
  > [Description](../features/topic_07_technical_debt/TECH-007/TECH-007_design.md) | The `_prompt_render.py` rendering functions use raw f-strings with no escaping. While currently safe because all inputs are internally system-generated, this must be hardened before any feature injects user-generated content directly into `add_context()` labels or block contents. D-INTL-06 sidesteps this by using `json.dumps()` for its specific memory blocks, but the base builder is still vulnerable to XML injection if fed raw user strings. Needs structured injection defenses (XML/CDATA/JSON) built directly into `PromptBuilder` and XML attribute escaping in render layers. Discovered during D-INTL-06 Red Team Cycle 1. _(2026-07-21: **unaffected by the middle-way direction** — escaping is a pure envelope concern (`C-INTL-06`) and is required in both `oneshot` and agentic modes; if anything, externalized content files raise its priority.)_

* **`TECH-012` 🟢: Multi-Step Git-Worktree Isolation is Broken (Reconcile Never Commits; Crashes on Step 2)**
  > [Description](../features/topic_07_technical_debt/TECH-012/TECH-012_design.md) | _Origin: `INT-US-03 SF-03` implementation-plan Phase-0 spike (2026-07-19)._ | The per-step worktree model was non-functional for multi-step untrusted loops: the reconcile never committed, and the second isolated step crashed. **✅ RESOLVED (2026-07-21) by `C-EXEC-06`** (per-run/session worktree isolation) — retained as the record of the defect and of what fixed it.

* **`TECH-013` 🔴: API Composition Roots Do Not Resolve Worktree-Isolation Policy**
  > [Description](../features/topic_07_technical_debt/TECH-013/TECH-013_design.md) | _Status: STUB. Origin: recorded during `C-EXEC-06 SF-03` implementation-plan Phase 4 (2026-07-20)._ | `C-EXEC-06 SF-03` wired session-isolation policy on the **CLI** composition root only. The API run sites (`interfaces/api/v1/pipelines.py`) resolve neither `enforce_isolation` nor the session policy, so a run started through the REST API silently gets a different execution posture than the same run started from the CLI. `apply_session_policy` was written to be reusable verbatim here.

* **`TECH-017` 🔴: Integration-Contract Proof Audit (Test Tier Must Match Story Tier)**
  > [Description](../features/topic_07_technical_debt/TECH-017/TECH-017_design.md) | _(new, 2026-07-26 — user mandate after INT-US-21 SF-02 CB-1 shipped 16 unit tests and **zero** integration/e2e)_ | **An `INT-US-NN` story is an integration contract, so its proof must be integration and e2e tests.** Unit tests are correct for TDD of a *unit* and belong in an integration story only to fix a specific behaviour or fill a narrow gap found while integrating. The corollary is the valuable half: **if an integration story needs heavy unit testing, the capability stories it was integrating shipped incomplete** — INT-US-21 proved this three times (handlers never registered → `D-INTL-02`; `RunContext.plan` documented as hook-populated with zero writes in `src/` → `D-INTL-03`; a required enum that cannot be YAML-serialized → `C-INTL-01`). So the audit must raise findings against the **capability** stories too, not just the contracts. Measured 2026-07-26 across 9 delivered contracts and 148 integration/e2e files: **`INT-US-25` is ✅ with `Verifiable Proof: [Pending]`**; `INT-US-01/04/05` cite a pre-existing *capability* suite rather than a contract journey; `INT-US-03` (3 tests) and `INT-US-09` (6) are happy-path only with no failure mode; `INT-US-02`'s E6/E7 were vacuous until 2026-07-25 (exit-code-only assertions, a fixture that could never pass its own battery, and **live paid API calls in a "mocked" test**); `INT-US-24` (9) and `INT-US-28` (20) are the standard to hold the rest to. Systemic: **graceful shutdown effectively unproven** (`CancelledError` 0 files, `atexit` 0, and the only SIGINT e2e skips on Windows) while the runner saves handover and flushes telemetry in a `finally:`; **11 of 43** flow/workflow files have zero failure-path tests — including the sole fan-out integration test guarding the `TECH-014` race; **12 tests verify essentially nothing** (3 with no assertion at all; a zombie-process test asserting only an exit code); and the `pytest.skip` path guard that silently killed two tests for months is still armed. Deliverable: a per-story matrix of contract *claims* vs what a test *proves*, each unproven claim filed. Findings become NEW stories/tickets — never edits to delivered entries. Ships with a tier-ratio guardrail at **planning** time: recording the rule as a review check on 2026-07-25 did not stop SF-02 CB-1 being planned unit-only the next day. _(2026-08-01: one piece of the 2026-07-26 evidence above is now stale — `test_cqrs_e2e.py::test_story_9_sigint_survival` no longer skips on Windows; `_signals.py` now routes Ctrl+Break (`SIGBREAK`) through the same graceful-cleanup handler as SIGINT/SIGTERM, so the test branches per-platform instead. This closes one instance of "graceful shutdown effectively unproven," not the broader audit — the rest of this finding (11/43 files, 12 vacuous tests, the armed `pytest.skip` guard, the per-story matrix deliverable) is untouched and TECH-017 remains 🔴.)_

* **`TECH-018` 🔜: Delivered Add-On Re-Validation Against an Integrated Base (INT-US-21-SUB / C-INTL-01)**
  > [Description](../features/topic_07_technical_debt/TECH-018/TECH-018_design.md) | _Status: STUB. Origin: INT-US-21 design `AD-9` (user mandate, 2026-07-25), relocated out of the feature on 2026-07-26 during the INT-US-21 scope re-cut._ | `C-INTL-01` / `INT-US-21-SUB` (Iterative Decomposition) was delivered ✅ claiming coverage by "`pytest -m integration` and the `FeatureDecomposer` suite" — a claim never exercised through a real `sw run feature_decomposition` journey, because **no such journey could run**: `draft+feature`/`validate+feature` were unregistered, so the shipped pipeline died at step 1. INT-US-21 then moves the ground underneath it, introducing seams the add-on never saw: `RunContext.decomposition` (the decomposition plan no longer travels on `context.plan`), the persisted `<stem>_decomposition.yaml` schema, approve-on-resume semantics, stub component spec paths, and the CLI journey itself. Audit the three questions with evidence: is the claimed scope still valid, does it still cover what US-21 needs, does it cooperate with the new seams. **Audit + report ONLY** — no remediation, and no edits to `INT-US-21-SUB`'s entry or docs (finished-stories-immutable); every finding becomes a NEW story or ticket. Sequence **after** INT-US-21 SF-03 is committed, since the integrated base is what it audits against. Was `AD-9`, a gate on INT-US-21 going 🟢; an audit of a different, delivered story must not hold this epic's closure hostage, and an audit of unknown size must not sit on a critical path. Coordinate with `TECH-017` so the two do not double-cover `INT-US-21-SUB`.

* **`TECH-029` 🟢: Sandbox Process Cap Uses `RLIMIT_NPROC`, Which Bounds the User and Not the Sandbox**
  > [Description](../features/topic_07_technical_debt/TECH-029/TECH-029_design.md) | _(2026-08-12 — found diagnosing 29 Linux test failures after the Windows move; accounts for 18 of them. Analysis: `docs/analysis/linux_test_failures_2026-08-12.md`.)_ | `atom.py:24` caps every bash step at `max_processes=128`; the Unix limiter maps that to `setrlimit(RLIMIT_NPROC, 128)` via a `preexec_fn` that `executor.py:156` applies only when `sys.platform != "win32"`. **`RLIMIT_NPROC` is per-real-UID and system-wide** — it caps every process the invoking user owns, not the processes this sandbox spawns, so the field does not bound what it exists to bound and fails whenever the user is running anything else. Windows maps the same field to a Job Object, where "max processes" does mean *processes in this job*: two platforms implement different constraints under one name and only the Linux one is wrong. Reproduced — `bash gen.sh` returns 0 unlimited and **254** under the limit, with `fork: retry: Resource temporarily unavailable` on the script's first fork; `RLIMIT_AS` alone is harmless because a shell builtin never forks, which is why a naive probe misses it. Measured: `max_processes=None` takes integration 14→4 failures and e2e 9→1, wall clock 67s→12s and 39s→14s. **Never surfaced before because the `preexec_fn` branch had never executed on Windows** — any limit existing on one platform only is untested on the other by construction. **Not a test defect**: the 18 tests assert worktree-isolated runs complete and reconcile, which is `C-EXEC-06`'s promise, and must not be weakened. Fix shape: stop setting `RLIMIT_NPROC` on Linux and log it unenforced (keeping `RLIMIT_AS`/FR-11), with cgroups v2 `pids.max` as a separate story for real enforcement; raising the number is rejected as it converts a reproducible failure into an intermittent one. Out of scope: the memory limit; the other 11 Linux failures (unrelated causes); container isolation. Deliberately **not** folded into `TECH-025` SF-05 — mixing a live `src/` change into a citation commit is the attribution problem that ticket exists to remove.

* **`TECH-030` 🟢: An Empty `FolderGrant` Path Grants the Whole Project on POSIX and Nothing on Windows**
  > [Description](../features/topic_07_technical_debt/TECH-030/TECH-030_design.md) | _(2026-08-12 — found working through the Linux failures; Cluster D of `docs/analysis/linux_test_failures_2026-08-12.md`.)_ | `FolderGrant("", READ, recursive=True)` means two different things per OS. `_path_matches_grant` compares path **segments**, and `_resolve_access` builds an absolute path first: on POSIX `/tmp/proj/x` splits to `['', 'tmp', …]` whose first element matches an empty grant's `['']`, while on Windows `C:/proj/x` splits to `['C:', …]` and never matches. Measured with that single grant: reads of `src/domain/billing/calc.py`, **`secrets/prod.env` and `.git/config` all succeed** though granted nowhere; with a normal `src/domain/billing` grant the same `secrets/prod.env` read is correctly refused. **Scope is the project root, not the filesystem** — `/etc/passwd` is refused by a separate containment check, so this is an unintended whole-project read rather than an escape. Invisible for years because `test_grant_at_root_covers_everything` asserts the Windows reading, on the platform where the branch is unreachable. Same shape as `TECH-029`: one configuration, two platform behaviours, hidden until the code ran somewhere new. **The ticket exists to decide, not to make the test pass**: either an empty grant path is invalid and rejected at construction (matching the test and Windows, failing closed), or it legitimately means "project root" and Windows is the broken platform — both require a production change, and editing the test would silently bless whole-project read. Fix belongs in `_path_matches_grant`/`_resolve_access`, plausibly by comparing resolved `Path` objects with `is_relative_to` instead of string segments, which removes the whole class of root-segment bugs. Note the sibling assumption on the same line — `target.replace("\\", "/")` treats a backslash as a separator, which contradicts the user's 2026-08-12 ruling that a backslash is a legal filename on Linux. Out of scope: the absolute-path containment check; `TECH-029`; the two Cluster D tests already fixed.

* **`TECH-032` 🔴: The Non-Python QA Runners Report an Absent Toolchain as Success**
  > [Description](../features/topic_07_technical_debt/TECH-032/TECH-032_design.md) | _(2026-08-12 — found during `TECH-031`, which fixed the same defect in the Python runner; filed separately because the cause differs.)_ | A runner whose tool is not installed returns a **clean result** rather than an error: the subprocess exits non-zero with empty stdout, the parser sees nothing to report, and "nothing to report" is indistinguishable from "nothing wrong". **The gate certifies a run that never happened** — the same family as `TECH-017`. **Measured** by injecting `exit_code=127, stdout=""` into each runner's executor: **13 paths report success** — `JavaRunner` (`run_tests`, `run_linter`, `run_complexity`, `run_architecture_check`), `RustRunner` (the same four with `run_compiler` for `run_architecture_check`), `KotlinRunner` (`run_tests`, `run_linter`, `run_complexity`) and `TypeScriptRunner` (`run_architecture_check`, `run_compiler`). `TypeScriptRunner.run_compiler` is the sharpest case: it **has** an `except FileNotFoundError` guard and the guard is **dead**, because `SubprocessExecutor` returns `exit_code=127` rather than raising — a guard testing the wrong thing reads as coverage while providing none, exactly like the `shutil.which("tach")` host-PATH check `TECH-031` found. **Not `TECH-031`**: that ticket's cause is the container prepare phase failing to build a venv, and these runners never go through it — same symptom, unrelated cause. Fix shape: `did_not_run(result, tool)` (`sandbox/language/core/python/toolchain.py`) is already language-agnostic and should move somewhere shared; **preserve its discriminator — empty stdout, not the exit code**, since per-language exit conventions differ but "printed nothing" does not. Ship a parameterized guardrail across every runner and method, or a sixth language regrows this. Out of scope: the declared `STUB` methods that never call the executor (which is why the guardrail cannot naively assert over every method); the Python runner (`TECH-031`); the prepare phase (`TECH-031`). One runner per commit, never bundled into a feature commit.

* **`TECH-033` 🟢: A Step's Retry Budget Resets on Every `sw resume`**
  > [Description](../features/topic_07_technical_debt/TECH-033/TECH-033_design.md) | _(named as an accepted inherited limit in `INT-US-21` NFR-2 2026-07-27; carried forward by `TECH-020` 2026-08-12, which made it visible but could not change behaviour; minted 2026-08-12)_ | `gate.max_retries` is checked against a counter that lives only in memory: `_execute_loop` builds a fresh `LoopState` on every entry (`engine/runner.py:243`) whose `attempts` starts empty, and `resume()` re-enters that loop — so **every `sw resume` grants a full fresh budget**. `max_retries: 3` bounds retries *per session*, not per step; across N resumes a step can run 3N times. `_handle_retry` and `_handle_loop_back` share the counter, so the reset widens both. **Two claims in the existing record are wrong, and are why it was never fixed** (measured 2026-08-12): NFR-2 parks it as *"persisting attempt counters is a state-schema change; `C-FLOW-07` territory"*. (1) **Not a schema change** — `StepRecord.attempt` already exists, is already written by `_handle_retry`, and **round-trips through the store** (verified by save/load); only the *read* is missing, nothing seeds `attempts` from it. (2) **`C-FLOW-07` would never have fixed it** — it is *HITL Root-Cause Tagging*, a human tagging why a pipeline failed for the Friction Analytics engine. No capability anywhere covers retry-budget persistence, so this sat parked against a ticket that does not cover it. Fix shape: seed `LoopState.attempts` from the persisted `StepRecord.attempt` — but **close the write side first**, because `_handle_loop_back` spends the budget and never records it, so the persisted counter is already incomplete and seeding alone would fix `retry` while silently leaving `loop_back` resetting. **Settled (user, 2026-08-12): this is a bug, not documented behaviour** — `max_retries: 3` means three attempts for that step *ever*; a resume continues a run, it does not re-grant a budget. So no gate-documentation change is needed: the rule already stated is the correct one and the code does not honour it. Proof must **resume** a run whose budget is spent — every existing retry test lives inside one `_execute_loop` entry and so cannot observe the reset. Out of scope: `max_retries` defaults and pipeline YAML; token-spend budgeting (`B-FLOW-05`, a different mechanism that does not substitute); reworking gate evaluation beyond the two counter sites.

## Documentation & Knowledge Architecture
* **`TECH-026` 🟢: Roadmap Placement Contract — One Registry ID, One Line**
  > [Description](../features/topic_07_technical_debt/TECH-026/TECH-026_design.md) | _(2026-08-08 — found during `TECH-025` SF-02, when the agent added TECH-025's seven design-document sub-features to `master_story_roadmap.md` after judging its entry "inconsistent" with `TECH-001`'s and `TECH-006`'s.)_ | No document states what belongs in `master_story_roadmap.md` versus a topic doc versus a feature's design. The convention is nearly universal in the file — **one registry ID = one line** (ID, short name, link, status flag), with a design's `SF-NN` decomposition never appearing — but is written down nowhere, so an agent derives it from whatever it greps. `B-INTL-09` has 4 sub-features and one line; `C-FLOW-02` has 1 and one line. Measured violations: **18 nested SF lines across 5 entries** — `TECH-001` (4), `TECH-005` (3), `TECH-006` (2), `TECH-009` (2) and `TECH-025` (7, added by the session that found this and reverted there). **Every one is in the TECH family; zero US-N or capability entries deviate** — so an agent hunting precedent inside `TECH-NNN` finds the convention broken four times and never reaches the ~28 clean entries. The trap is that two different things are spelled "SF": `INT-US-21-SF01` is a minted sub-story add-on and belongs, while `SF-01` in a Sub-Feature Breakdown is internal decomposition with no ID and never belongs. Compounded by `phase-6-documentation.md` §6.3.1 ordering the agent to "check off boxes for the User Story and any **Add-Ons** you implemented" — correct if "Add-Ons" means sub-story IDs, a standing order to corrupt the roadmap if read as sub-features, and nothing disambiguates it. Same defect class as `TECH-019` (instructions not matching reality, executed as truth) and the same fix shape: write the contract once as a shared reference under `specweaver-ticket`, point `phase-6-documentation.md` / `phase-5-document.md` / `ticket/SKILL.md` at it **in both `.agents/` and `.claude/` trees** (`check_skill_sync.py` enforces byte-identity), ship `scripts/check_roadmap_placement.py` in the `doc` gate, and repair **all four offenders — `TECH-001`, `TECH-005`, `TECH-006`, `TECH-009`** _(corrected 2026-08-11: this sentence previously named only two of the four its own table lists)_. **Re-measured 2026-08-11: the repair list is 11 lines across 4 entries**, since `TECH-025`'s seven were reverted by the session that added them. The checker's rule is *a line exists only for something with its own registry ID* — validated as "a third-level list item must name a bold registry ID", which separates 168 legal lines from exactly the 11 violations; a lexical `INT-US-NN-SFxx`-vs-bare-`SF-NN` test was rejected because it flags 15 legal prose mentions and ignores ~89 capability lines. Out of scope: why TECH tickets get full `###` sections where capabilities get one line; general skill rewrites; and the `SF-NN` identifier format and ownership rules, which are `TECH-027`'s.

* **`TECH-027` 🟢: Sub-Feature Identifier Contract — Two Digits and an Explicit Owner**
  > [Description](../features/topic_07_technical_debt/TECH-027/TECH-027_design.md) | _(2026-08-11 — raised by the user while reviewing `TECH-026`. Split out rather than folded in: `TECH-026` repairs 11 lines in one file, this reaches 40 documents across 5 delivered stories.)_ | `SF-NN` is used project-wide and has never been given a contract, producing two compounding defects. **Format:** `SF-{2d}` is already the norm — 166 padded filenames against **16 unpadded** (`TECH-005` 5, `TECH-002` 4, `C-EXEC-02` 3, `E-EXEC-01` 2, `TECH-007` 2) — plus single-digit prose in **40 files** reaching beyond `roadmap/` into `dev_guides/` and `architecture/`. **Ownership:** `SF-01` exists in `TECH-001`, `TECH-005`, `TECH-006`, `TECH-009`, `TECH-025`, `B-FLOW-01` and more, so outside `features/<topic>/<STORY-ID>/` — where the path supplies the owner — a bare `SF-NN` is a dangling reference that reads as resolved. Two live instances in `master_story_roadmap.md`: line 684 (inside **TECH-001's** section) reads "`TECH-025` tracks a … citation gap in SF-01/02/03", where the sub-features meant are TECH-001's but the only story named is TECH-025 — a reader binds to the nearest ID and gets the claim backwards; line 716 repeats the shape inside TECH-005's section and is unpadded too, both rules failing in one line. The convention **already exists for `FR-N`** — `TECH-025_design.md:8` states it and three further documents hand-copy it — invented ad hoc, scoped to one ticket, enforced nowhere, and never noticed to apply to `SF-NN`. Same defect class as `TECH-019`/`TECH-026`; same fix shape: state both clauses once in `TECH-026`'s shared reference, retire the four hand-copied `FR-N` notes into it, ship `scripts/check_sf_identifiers.py` in the `doc` gate (clause 2 is decidable from the file path alone), rename the 16 files one story per commit, and waive finished-stories-immutable explicitly. **Sequencing:** collides with `TECH-025` SF-05, whose drafted plan already works around `TECH-002_sf1_…`; decide the order before SF-05 starts. Out of scope: `INT-US-NN-SFxx` sub-story IDs, any renumbering, `TECH-026`'s placement rule, and the `AD-N`/`NFR-N`/`CB-N` families.

* **`TECH-019` 🟢: Skill Instruction Integrity — Dangling Doc References and Contradictory Gate Orders**
  > [Description](../features/topic_07_technical_debt/TECH-019/TECH-019_design.md) | _Status: DELIVERED 2026-08-08 (`ffaa4a8b` repairs, `fdc4eac2` guardrail). Origin: found by the INT-US-21 SF-02 CB-1 pre-commit gate (2026-07-26)._ Design found **twelve** dangling references, not six — `phase-6-documentation.md` also ordered updates to `docs/quickstart.md` and `docs/developer_guide.html`, which have never existed. Blame reversed the stub's read of Defect 2: §1.9 has one commit and §2.8 four since, so §2.8 survived and §1.9 now defers to it. Guardrail: `scripts/check_skill_references.py`, wired into the `doc` gate. | Skill instructions are never checked against the repo they instruct on, so they rot silently and the agent absorbs the rot as truth. **`TECH-008` deleted `docs/architecture/architecture_reference.md`; six LIVE instruction sites still order the agent to read it** — `.agents/AGENTS.md:46`, `specweaver-design/phase-2-research.md:24`, `specweaver-implementation-plan/phase-1-preparation.md:20` and `phase-3-architecture.md:22`, `specweaver-pre-commit/phase-1-architecture.md` (§1.1 read **and §1.8 as the place to record new boundary violations**, when the live ledger is `06_lessons_and_future/known_boundary_violations.md`) and `phase-6-documentation.md:27`. So every design, implementation-plan and pre-commit run since `TECH-008` has been told to load architecture context that cannot load — the agent either silently skips the step its phase depends on, or fills the gap from training data and reports architecture it never read. Second defect: `phase-1-architecture.md` §1.9 says the combined analysis MUST be printed in chat and MUST NOT be an Artifact, while `phase-2-test-gap.md` §2.8 says the opposite — §1.9 relocated the gate and §2.8 was never updated, so compliance is a coin flip and either choice violates a MUST. Fix = repair the six references, reconcile the two format orders (delete the loser), and **ship the guardrail**: `scripts/check_skill_references.py` asserting every repo-relative path in a skill instruction resolves on disk — the same invariant class as the handler-reachability test in `f7a0f34f`, *a declared reference must resolve*, applied to instructions. Out of scope: skill rewrites, and historical implementation plans/designs that mention the old path (records of what was true then — finished-stories-immutable). Low risk, high blast radius: should land ahead of the next feature's design phase rather than queueing behind `TECH-017`/`TECH-018`. One commit for repairs, one for the checker.

* **`TECH-008` 🟢: Architectural Documentation Modularization**
  > [Description](../features/topic_07_technical_debt/TECH-008/TECH-008_design.md) | A severe structural refactoring of the monolithic `docs/architecture` directory. Slices the 46KB `architecture_reference.md` and 17 loosely organized files into a visually-rich, GitHub-publishable static site structure perfectly aligned with Domain-Driven Design (Hexagonal Layers, Bounded Contexts). Uses a Non-Destructive Copy-and-Verify strategy to guarantee zero data loss. Formalizes the Composition Root vs Factory debates into ADRs.


## Build & Packaging
* **`TECH-028` 🟢: Split `dev` Dependency Definitions — Broken Default Sync, Test Tooling in the Container Image**
  > [Description](../features/topic_07_technical_debt/TECH-028/TECH-028_design.md) | _(2026-08-11 — found rebuilding the environment on a Linux server after the Windows laptop failed; a plain `uv sync` produced an environment in which 5347 tests errored.)_ | `pyproject.toml` declares **two different things named `dev`**: `[dependency-groups] dev` (`:139-146` — `pytest-xdist`, `complexipy`, `respx`, two `types-*`) and `[project.optional-dependencies] dev` (`:60-69` — `pytest`, `pytest-cov`, `pytest-asyncio`, `ruff`, `mypy`, `tach`, `httpx`, `respx`). `respx` sits in both, and the runner is separated from its plugin: `pytest-xdist` in the group, `pytest` in the extra. Because `uv` installs groups by default and extras only on request, **the project's own default command yields an environment that cannot work** — measured: `uv sync` → 5347 errors, `uv sync --extra dev` → 5442 passed, `uv sync --all-extras` → 5567 passed. It fails loudly rather than visibly: `pytest` resolves transitively via `xdist` so the suite runs and reports mass failure, and under `pytest 9` the absent `pytest-asyncio` is a hard error rather than a warning. Second consequence: `Containerfile:25` runs `uv sync --all-extras --no-dev --frozen`, where `--no-dev` drops the group but `--all-extras` pulls the `dev` extra back in — dry-run verified that flag pair removes exactly `complexipy` and `pytest-xdist`, so **the image built from it contains `pytest`, `ruff`, `mypy` and `tach`** while omitting the two a developer wants. _(No full containerized support exists yet — user, 2026-08-11 — so this is a latent defect in an unfinished build path, not a live production leak; the flag pair is still wrong wherever containerization lands.)_ **Not a documentation gap:** `README.md:95`, `CONTRIBUTING.md:16` and `docs/user_guides/1_installation_and_setup.md:15` already all say `uv sync --all-extras`; the docs have been compensating for a wrong default. Fix shape: collapse both into `[dependency-groups] dev`, keep extras for genuine optional features (`openai`/`anthropic`/`mistral`/`qwen`/`all-llm`/`serve`), correct the `Containerfile` to `--extra serve --no-dev`, and ship a guard asserting no name is both a group and an extra. Manifest and container change land in **one commit** with a rebuilt image. Out of scope: the Python 3.14 / `uv.lock` re-resolution question; the 6 tests still failing on Linux; any dependency version bump; migrating away from `uv`.

* **`TECH-031` 🟡: The Container Prepare Phase Has Never Installed a Toolchain**
  > [Description](../features/topic_07_technical_debt/TECH-031/TECH-031_design.md) | _(2026-08-12 — found during `TECH-028`; renamed and re-scoped the same day once measured. Was "…Assumes a Target Project's Dependency Layout", which named the last defect of three.)_ | **Three chained defects, reproduced against live podman, not inferred.** (1) The prepare phase **cannot create a venv at all**: `/workspace` is mounted `:ro` and the container is `--read-only`, but `uv sync` writes `.venv` into the workdir — `failed to create directory /workspace/.venv: Read-only file system`, exit 2, **identical for both dependency layouts**, and only `logger.warning`'d so nothing surfaces it. A target with no `uv.lock` fails one step earlier still, on `failed to write to file /workspace/uv.lock`, which the current fallback from `uv.lock` to `pyproject.toml` walks straight into. (2) The **execute phase would not use the venv anyway** — `_build_container_cmd` sets no `PATH` and no `UV_PROJECT_ENVIRONMENT`, so `python -m pytest` resolves to the image's interpreter; fixing (1) alone changes nothing observable. (3) The **extras-layout gap** this ticket was originally written about, real but third in the chain: with (1) removed, `[dependency-groups]` installs pytest and `[project.optional-dependencies]` does not. **This corrects `TECH-028`'s claimed side-effect** — it did not incidentally fix the phase for SpecWeaver, which hits (1) like everything else. **Delivered:** the fourth defect, and the reason none of the above was ever noticed — `run_tests`, `run_linter` and `run_architecture_check` all reported an **absent toolchain as a clean run**, a vacuous proof inside the QA gate itself (`sandbox/language/core/python/toolchain.py`, `did_not_run`; the discriminator is empty stdout, not the exit code — pytest exits 4 for a `tests/` directory that does not exist yet). Container QA now fails loudly instead of silently, and `execution_mode` defaults to `"host"`, so the remaining chain is **latent, not live**. Out of scope: `--all-extras` (rejected — widening what untrusted code installs cuts against the sandbox's purpose); this repo's manifest (`TECH-028`); `B-EXEC-01`'s status; the non-Python runners' own silent-success shape (`TECH-032`). Adjacent, recorded because nobody has looked: `check_fr_coverage.py B-EXEC-01` exits 1 — it was never a `TECH-025` subject, so its nine requirements have never been audited, and it is the story that shipped this phase.
